librosa==0.11.0
numpy==2.1.3
soundfile==0.14.0
torch==2.12.1
torchaudio==2.11.0
torchcrepe==0.0.24
demucs==4.1.0

# 코드 인식(poc_chord_detect.py)용 사전학습 모델. Cython 확장을 빌드해야 해서
# Windows에서는 Visual C++ Build Tools(C++ 워크로드)가 먼저 설치되어 있어야 함:
# winget install --id Microsoft.VisualStudio.2022.BuildTools --override "--add Microsoft.VisualStudio.Workload.VCTools"
# 설치 시 --no-build-isolation 필요(안 그러면 cython을 못 찾음): pip install cython madmom --no-build-isolation
# madmom 0.16.1은 2018년 배포라 Python 3.10+/numpy>=1.24와 호환 안 되는 부분이 있어
# poc_chord_detect.py의 _patch_madmom_py312_compat()에서 임포트 전에 몽키패치로 메꿈.
cython
madmom==0.16.1
mido
